# 新能源预测与负荷预测改进说明

本次改进面向 `PowerTool` 的日前负荷预测与新能源预测功能，重点增强配置开放性、算法可选性、预测结果可解释性和界面呈现能力。

## 1. 内置 JSON 配置

新增 `data/forecast_builtin_config.json`，集中维护：

- 默认算法、默认预测间隔、预测时长、综合方案候选算法；
- 算法目录，包括自适应综合方案、岭回归、Huber 鲁棒回归、逐时相似日、指数平滑、季节朴素、随机森林、梯度提升；
- 负荷、新能源常见字段别名，增强 CSV 解析兼容性；
- 区域模板和特殊事件模板。

同时完善 `data/forecast_holidays.json`，增加中国节假日、调休工作日和美国节假日定义。

## 2. 用户可选算法

GUI 中新增“算法”下拉框，用户可以显式选择算法。预测引擎支持以下模式：

- `adaptive_ensemble`：自适应综合方案；
- `ridge`：岭回归；
- `huber`：两阶段 Huber 权重岭回归；
- `hourly_analog`：逐时相似日；
- `exp_smoothing`：指数平滑；
- `seasonal_naive`：季节朴素；
- `random_forest`：随机森林；
- `gradient_boosting`：梯度提升。

自适应综合方案会对候选算法进行留出校验，并按误差反比分配综合权重。

## 3. 新能源预测增强

- 新能源资源类型明确区分为光伏和风电，不再把聚合功率直接作为默认资源；
- 光伏预测加入夜间物理约束，夜间 P10/P50/P90 均强制为 0；
- 风电预测加强风速、风向、气象因子特征；
- 预测值支持装机容量上限约束；
- 支持 24 点、48 点和 96 点等不同时间分辨率输出。

## 4. 负荷预测增强

- 支持气象、节假日、周内周期、季节周期、相似日等特征；
- 支持特殊事件修正，包括绝对 MW 修正和相对比例修正；
- 支持预测区间 P10/P50/P90；
- 输出峰值、谷值、峰谷差、平均负荷、负荷率、爬坡量、日电量等日特性指标。

## 5. 界面改进

“负荷/新能源预测”页签改为更接近调度类系统的呈现方式：

- 参数区新增预测类型、资源类型、算法、分辨率、日期、经纬度、高程、容量等配置；
- 结果区新增曲线、24/96 点明细、算法与日特性三个子页签；
- 明细表显示时刻、P10、预测值、P90、温度、GHI、风速、驱动因素；
- 算法页展示各算法 MAE、RMSE、MAPE、权重及日特性指标；
- 增加 JSON、CSV 导出按钮，便于工程调试和结果归档。

## 6. 验证

本次修改后执行：

```bash
pytest -q
```

结果为：`74 passed, 1 skipped`。

## 5.1 太阳角度辅助界面（新增）

针对新能源预测中的光伏场景，新增“太阳角度分析”辅助功能，主要能力如下：

- 基于用户输入的经纬度、日期和时间，自动按照当前经度对应的标准时区（如东经 120°附近自动采用 UTC+8）计算太阳位置；
- 显示当前太阳高度角、天顶角、方位角、太阳时、时差方程与参考晴空辐照度；
- 给出当日日出、日落、太阳正午时刻，以及正午时刻的太阳高度角；
- 以高可视化方式展示全天太阳轨迹，包括：
  - 太阳穹顶极坐标轨迹图（0°=北，90°=东，180°=南，270°=西）；
  - 全天太阳高度角曲线；
  - 参考晴空 GHI 曲线；
  - 日出、正午、日落、当前时刻的标注线。

该功能可作为光伏出力预测、运行监视和调度人工修正的辅助界面。

## 5.2 南京默认站点、天气修正、组件倾角/方位角与预测联动（新增）

本次在新能源预测中进一步增强了光伏物理约束与可解释性：

- 经纬度默认值改为中国南京：纬度 32.0603°N、经度 118.7969°E、海拔 20 m，节假日默认采用 CN；
- 内置 JSON 增加南京默认光伏站点、南京默认负荷站点、天气场景和光伏组件几何默认值；
- 新增天气修正：晴空、少云、多云、阴天、雨雪、雾霾，并支持用户输入云量和辐照人工系数；
- 新增光伏组件倾角、方位角、地表反照率、组件温度系数输入；
- 新增倾斜面辐照度 POA 计算，采用 0°=北、90°=东、180°=南、270°=西的方位角约定；
- 太阳角度辅助界面现在同时显示晴空 GHI、天气修正 GHI、POA、直射/散射分量、入射角和相对功率修正系数；
- 新能源预测算法特征向量已加入太阳高度角、太阳方位角、POA、入射角余弦、组件倾角/方位角等特征；
- 光伏预测后处理已按天气、组件几何和组件温度系数进行物理修正；
- 新能源预测曲线页增加 GHI/POA 叠加显示，明细表新增 POA、太阳高度角、方位角、入射角、天气系数和 PV 修正系数。

该功能使光伏预测结果与太阳几何、天气状态和组件安装方式直接联动。对于阴天、雨雪、雾霾、组件朝向偏离正南、倾角不合理等情况，预测曲线会相应发生变化，不再仅依赖历史功率序列外推。

## 5.2 太阳辅助界面交互与显示修正（新增）

根据界面截图反馈，对新能源预测页的太阳角度辅助功能进行了布局修正：

- 左侧参数区不再放置“太阳角度分析时刻”和“太阳角度分析”按钮，避免预测参数区过长、按钮拥挤；
- “太阳角度/轨迹”页签右下方新增分析时刻输入框、刷新按钮和日照区间滑条；
- 滑条自动限定在当日日出到日落时间之间，用户可拖拽选择当前分析时刻；
- 右侧太阳高度曲线区支持鼠标拖拽选择当前时刻；
- 太阳轨迹文字区高度降低，图形区留出更多空间；
- 图形边距重新调整，避免右侧辐照度纵轴、图例和标题被裁切；
- Matplotlib 图例和轴标签中的辐照度单位改为 ASCII 形式 W/m2，避免部分字体无法显示上标字符导致方框乱码。
