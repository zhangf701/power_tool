# 计算模块审查说明

本次审查重点覆盖非预测类计算模块：短路电流、录波/COMTRADE 分析、线路参数标幺值、架空线几何序参数、导线弧垂、配网合环、AVC 策略、小扰动稳定、暂态稳定和近似公式模块。

## 已修正的问题

1. `power_tool_comtrade.sequence_components` 原先只使用 A/B/C 三相 RMS 幅值做序分量变换，丢失相角信息。对完全平衡三相波形会错误得到“零序=相 RMS、正序≈0”。新版改为先提取基波相量，再进行对称分量变换；GUI 录波分析页也改为传入采样率和基波频率。

2. `power_tool_faults.short_circuit_capacity` 原先对 B/C 单相接地、AB/CA 两相短路、AB/CA 两相接地等非默认相别故障没有正确旋转序分量。故障电流幅值可能相近，但故障相电压边界条件不满足，界面显示的故障相会错位。新版已按相别修正：
   - B 相、C 相接地时，对应故障相电压在金属性接地时为 0；
   - AB、BC、CA 两相短路时，对应两相电压相等；
   - ABG、BCG、CAG 两相接地时，对应两相对地电压为 0，非故障相电流为 0。

3. 双电源短路模式下，断路器开断能力判断改为保守比较故障点总电流、左侧贡献和右侧贡献的最大值，避免只按故障点合成电流而低估侧端断路器开断电流。

## 新增回归测试

- 增加平衡三相录波序分量测试：正序约等于相 RMS，负序和零序接近 0。
- 增加相别故障边界条件测试：B/C 单相接地、AB/CA 两相短路、ABG/CAG 两相接地均校验故障边界。

## 仍建议后续增强的计算点

1. 三绕组变压器参数模块应增加“试验容量基准”选择项。当前实现对短路损耗做容量折算，但 Uk% 的来源约定仍依赖用户输入是否已统一到公共基准。工程应用中应显式选择“厂家已折算到高压容量”或“按两绕组较小容量给出”。

2. 三绕组变压器 T 型等值当前对分解后的电抗使用 X≈Z 近似，适合大型主变快速估算。若用于配变或损耗占比较高设备，应改为逐支路用 sqrt(Z²-R²) 计算，并对 R>Z 给出硬错误。

3. 短路模块目前用系统短路容量和 X/R 推算正序、负序、零序系统阻抗，零序系统阻抗没有独立输入。对接地故障精算，应增加 Z0_sys 或 R0/X0 输入。

4. COMTRADE 的 Prony 类估计仍属于快速估算，建议后续增加峰值包络法、Matrix Pencil/ESPRIT 或 TLS-Prony，并提供置信度。
